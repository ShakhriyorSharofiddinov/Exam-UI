import 'package:flutter/material.dart';

class EngEntranceTestPage extends StatelessWidget {
  const EngEntranceTestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}
