import 'package:flutter/material.dart';

class PrEntranceTestPage extends StatelessWidget {
  const PrEntranceTestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purple,
    );
  }
}
